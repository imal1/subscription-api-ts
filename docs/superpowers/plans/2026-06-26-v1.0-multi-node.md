# MioBridge v1.0 — 多节点分布式管理系统 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 MioBridge 从单机订阅转换服务升级为分布式多节点管理系统，支持控制面通过公网 + HMAC 签名管理 2-10 台远程节点。

**Architecture:** 新增 NodeManager 单例服务负责节点注册表加载、并发轮询、HMAC 签名请求和状态聚合。新增 KernelAdapter 接口统一三种内核。Dashboard 根据 nodes.yaml 存在与否自动切换单机/集群视图。远程节点通过 HMAC 中间件保护现有 API。

**Tech Stack:** Next.js Pages Router (Node runtime), TypeScript, ky (HTTP client), crypto (HMAC-SHA256), fs (yaml), React (Dashboard)

## Global Constraints

- 单机部署（无 nodes.yaml）行为完全不变，向后兼容
- 所有后端 service 使用单例模式（getInstance），与现有代码一致
- Dashboard 使用 Botanical Garden 主题（CSS 变量，不硬编码色值）
- 控制面使用 ky 发起远程 HTTP 请求（项目已安装）
- 文件权限：nodes.yaml 应为 600
- 远程节点超时 10s，轮询间隔 30s
- HMAC 时间窗口 ±30s，localhost 跳过验证
- 使用 `@/server/...` 别名引用后端模块

---

### Task 1: 扩展类型定义

**Files:**
- Modify: `frontend/src/server/types/index.ts`

**Interfaces:**
- Produces: `NodeConfig`, `NodeStatus`, `ClusterStatus`, `KernelAdapter` (interface), `KernelType`, `NodeEntry`

```typescript
// 在现有类型后追加以下内容：

/** 节点配置（来自 nodes.yaml） */
export interface NodeConfig {
  id: string;
  name: string;
  host: string;
  port: number;
  secret: string;          // HMAC 共享密钥，localhost 可为空
  kernel: KernelType;
  location: string;
  enabled: boolean;
}

export type KernelType = 'sing-box' | 'xray' | 'v2ray';

/** 单个节点的运行时状态 */
export interface NodeStatus {
  nodeId: string;
  name: string;
  kernel: KernelType;
  location: string;
  online: boolean;
  error?: string;           // 离线或异常原因
  latency?: number;         // 毫秒
  nodesCount?: number;      // 代理节点数
  subscriptionExists?: boolean;
  clashExists?: boolean;
  mihomoAvailable?: boolean;
  kernelAccessible?: boolean;
  version?: string;
  uptime?: number;
}

/** 集群聚合状态 */
export interface ClusterStatus {
  totalNodes: number;
  onlineNodes: number;
  totalProxies: number;
  nodes: NodeStatus[];
  lastUpdated: string;
}

/** 内核适配器接口 */
export interface KernelAdapter {
  readonly type: KernelType;
  getConfigPaths(): Promise<string[]>;
  extractNodeUrls(): Promise<string[]>;
  isAvailable(): Promise<boolean>;
}

/** nodes.yaml 顶层结构 */
export interface NodesYaml {
  nodes: NodeConfig[];
}
```

- [ ] **Step 1: 追加类型到 types/index.ts**

Run: `cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit src/server/types/index.ts 2>&1 | head -5`
Expected: No errors related to the new types.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/server/types/index.ts
git commit -m "feat(types): add NodeConfig, NodeStatus, ClusterStatus, KernelAdapter types"
```

---

### Task 2: 内核适配器

**Files:**
- Create: `frontend/src/server/services/adapters/kernelAdapter.ts`
- Create: `frontend/src/server/services/adapters/singBoxAdapter.ts`
- Create: `frontend/src/server/services/adapters/xrayAdapter.ts`
- Create: `frontend/src/server/services/adapters/v2rayAdapter.ts`

**Interfaces:**
- Produces: `KernelAdapter` interface, `SingBoxAdapter`, `XrayAdapter`, `V2rayAdapter` classes (all implement KernelAdapter)
- Consumes: `KernelType`, `KernelAdapter` from types (Task 1)

#### 2a: 接口定义

**Create:** `frontend/src/server/services/adapters/kernelAdapter.ts`

```typescript
// KernelAdapter 接口定义
// 每种代理内核实现此接口，提供配置路径发现和节点 URL 提取能力

import type { KernelAdapter, KernelType } from '../../types';

export type { KernelAdapter, KernelType };
```

#### 2b: SingBoxAdapter

**Create:** `frontend/src/server/services/adapters/singBoxAdapter.ts`

```typescript
import { exec } from 'child_process';
import { promisify } from 'util';
import { config } from '../../config';
import { logger } from '../../utils/logger';
import type { KernelAdapter, KernelType } from './kernelAdapter';

const execAsync = promisify(exec);

export class SingBoxAdapter implements KernelAdapter {
  readonly type: KernelType = 'sing-box';

  async getConfigPaths(): Promise<string[]> {
    // sing-box 233boy 安装的配置路径
    return ['/usr/local/etc/sing-box/config.json'];
  }

  async extractNodeUrls(): Promise<string[]> {
    const urls: string[] = [];
    for (const cfg of config.singBoxConfigs) {
      try {
        const { stdout } = await execAsync(`sing-box url ${cfg}`, {
          timeout: config.requestTimeout,
        });
        if (stdout && stdout.trim()) {
          urls.push(stdout.trim());
        }
      } catch (error: any) {
        logger.warn(`SingBoxAdapter: 获取 ${cfg} URL 失败: ${error.message}`);
      }
    }
    return urls;
  }

  async isAvailable(): Promise<boolean> {
    try {
      await execAsync('sing-box version', { timeout: 5000 });
      return true;
    } catch {
      return false;
    }
  }
}
```

#### 2c: XrayAdapter

**Create:** `frontend/src/server/services/adapters/xrayAdapter.ts`

```typescript
import * as fs from 'fs-extra';
import { logger } from '../../utils/logger';
import type { KernelAdapter, KernelType } from './kernelAdapter';

interface XrayConfig {
  outbounds?: Array<{
    protocol: string;
    settings?: {
      vnext?: Array<{
        address: string;
        port: number;
        users?: Array<{
          id: string;
          flow?: string;
          encryption?: string;
        }>;
      }>;
      servers?: Array<{
        address: string;
        port: number;
        method?: string;
        password?: string;
      }>;
    };
    streamSettings?: {
      network?: string;
      security?: string;
      wsSettings?: { path?: string; headers?: Record<string, string> };
      realitySettings?: {
        serverName?: string;
        publicKey?: string;
        shortId?: string;
      };
      tlsSettings?: { serverName?: string };
    };
    tag?: string;
  }>;
}

export class XrayAdapter implements KernelAdapter {
  readonly type: KernelType = 'xray';
  private configPath = '/usr/local/etc/xray/config.json';

  async getConfigPaths(): Promise<string[]> {
    return [this.configPath];
  }

  async extractNodeUrls(): Promise<string[]> {
    const urls: string[] = [];
    try {
      if (!(await fs.pathExists(this.configPath))) {
        logger.warn('XrayAdapter: config.json 不存在');
        return urls;
      }
      const raw = await fs.readFile(this.configPath, 'utf8');
      const cfg: XrayConfig = JSON.parse(raw);

      for (const outbound of cfg.outbounds || []) {
        const url = this.outboundToUrl(outbound);
        if (url) urls.push(url);
      }
    } catch (error: any) {
      logger.error(`XrayAdapter: 解析配置失败: ${error.message}`);
    }
    return urls;
  }

  private outboundToUrl(out: XrayConfig['outbounds'][number]): string | null {
    const tag = out.tag || out.protocol;
    const vnext = out.settings?.vnext?.[0];
    const server = out.settings?.servers?.[0];

    if (out.protocol === 'vless' && vnext) {
      const user = vnext.users?.[0];
      const host = vnext.address;
      const port = vnext.port;
      const uuid = user?.id || '';
      const flow = user?.flow || '';
      const net = out.streamSettings?.network || 'tcp';
      const sec = out.streamSettings?.security || 'none';
      const sni = out.streamSettings?.tlsSettings?.serverName
        || out.streamSettings?.realitySettings?.serverName || host;
      const params = new URLSearchParams();
      params.set('type', net);
      if (sec !== 'none') params.set('security', sec);
      params.set('sni', sni);
      if (flow) params.set('flow', flow);
      if (net === 'ws' && out.streamSettings?.wsSettings?.path) {
        params.set('path', out.streamSettings.wsSettings.path);
      }
      if (sec === 'reality' && out.streamSettings?.realitySettings) {
        params.set('pbk', out.streamSettings.realitySettings.publicKey || '');
        params.set('sid', out.streamSettings.realitySettings.shortId || '');
      }
      return `vless://${uuid}@${host}:${port}?${params.toString()}#${encodeURIComponent(tag)}`;
    }

    if (out.protocol === 'vmess' && vnext) {
      const user = vnext.users?.[0];
      const vmessObj = {
        v: '2', ps: tag, add: vnext.address, port: vnext.port.toString(),
        id: user?.id || '', aid: '0', scy: 'auto', net: out.streamSettings?.network || 'tcp',
        type: 'none', host: '', path: '/', tls: out.streamSettings?.security === 'tls' ? 'tls' : '',
      };
      if (out.streamSettings?.wsSettings?.path) {
        vmessObj.path = out.streamSettings.wsSettings.path;
      }
      return `vmess://${Buffer.from(JSON.stringify(vmessObj)).toString('base64')}`;
    }

    if (out.protocol === 'trojan' && server) {
      const password = server.password || '';
      const host = server.address;
      const port = server.port;
      const sni = out.streamSettings?.tlsSettings?.serverName || host;
      const net = out.streamSettings?.network || 'tcp';
      const params = new URLSearchParams();
      if (net !== 'tcp') params.set('type', net);
      params.set('sni', sni);
      params.set('security', 'tls');
      return `trojan://${password}@${host}:${port}?${params.toString()}#${encodeURIComponent(tag)}`;
    }

    if ((out.protocol === 'shadowsocks' || out.protocol === 'ss') && server) {
      const method = server.method || 'aes-256-gcm';
      const password = server.password || '';
      const host = server.address;
      const port = server.port;
      const userinfo = Buffer.from(`${method}:${password}`).toString('base64');
      return `ss://${userinfo}@${host}:${port}#${encodeURIComponent(tag)}`;
    }

    return null;
  }

  async isAvailable(): Promise<boolean> {
    try {
      return await fs.pathExists(this.configPath);
    } catch {
      return false;
    }
  }
}
```

#### 2d: V2rayAdapter

**Create:** `frontend/src/server/services/adapters/v2rayAdapter.ts`

```typescript
import * as fs from 'fs-extra';
import { logger } from '../../utils/logger';
import type { KernelAdapter, KernelType } from './kernelAdapter';

interface V2rayConfig {
  outbounds?: Array<{
    protocol: string;
    settings?: {
      vnext?: Array<{
        address: string;
        port: number;
        users?: Array<{
          id: string;
          alterId?: number;
          security?: string;
          flow?: string;
        }>;
      }>;
      servers?: Array<{
        address: string;
        port: number;
        method?: string;
        password?: string;
      }>;
    };
    streamSettings?: {
      network?: string;
      security?: string;
      wsSettings?: { path?: string; headers?: Record<string, string> };
      tlsSettings?: { serverName?: string };
      realitySettings?: {
        serverName?: string;
        publicKey?: string;
        shortId?: string;
      };
    };
    tag?: string;
  }>;
}

export class V2rayAdapter implements KernelAdapter {
  readonly type: KernelType = 'v2ray';
  private configPath = '/etc/v2ray/config.json';

  async getConfigPaths(): Promise<string[]> {
    return [this.configPath];
  }

  async extractNodeUrls(): Promise<string[]> {
    const urls: string[] = [];
    try {
      if (!(await fs.pathExists(this.configPath))) {
        logger.warn('V2rayAdapter: config.json 不存在');
        return urls;
      }
      const raw = await fs.readFile(this.configPath, 'utf8');
      const cfg: V2rayConfig = JSON.parse(raw);

      for (const outbound of cfg.outbounds || []) {
        const url = this.outboundToUrl(outbound);
        if (url) urls.push(url);
      }
    } catch (error: any) {
      logger.error(`V2rayAdapter: 解析配置失败: ${error.message}`);
    }
    return urls;
  }

  private outboundToUrl(out: V2rayConfig['outbounds'][number]): string | null {
    const tag = out.tag || out.protocol;
    const vnext = out.settings?.vnext?.[0];
    const server = out.settings?.servers?.[0];

    if (out.protocol === 'vmess' && vnext) {
      const user = vnext.users?.[0];
      const vmessObj = {
        v: '2', ps: tag, add: vnext.address, port: vnext.port.toString(),
        id: user?.id || '', aid: (user?.alterId || 0).toString(),
        scy: user?.security || 'auto', net: out.streamSettings?.network || 'tcp',
        type: 'none', host: '', path: '/',
        tls: out.streamSettings?.security === 'tls' ? 'tls' : '',
      };
      if (out.streamSettings?.wsSettings?.path) {
        vmessObj.path = out.streamSettings.wsSettings.path;
      }
      return `vmess://${Buffer.from(JSON.stringify(vmessObj)).toString('base64')}`;
    }

    // v2ray 也支持 vless / trojan / ss（233boy v2ray 分支），逻辑同 xray
    // 复用相同模式

    if (out.protocol === 'vless' && vnext) {
      const user = vnext.users?.[0];
      const host = vnext.address;
      const port = vnext.port;
      const uuid = user?.id || '';
      const flow = user?.flow || '';
      const net = out.streamSettings?.network || 'tcp';
      const sec = out.streamSettings?.security || 'none';
      const sni = out.streamSettings?.tlsSettings?.serverName
        || out.streamSettings?.realitySettings?.serverName || host;
      const params = new URLSearchParams();
      params.set('type', net);
      if (sec !== 'none') params.set('security', sec);
      params.set('sni', sni);
      if (flow) params.set('flow', flow);
      return `vless://${uuid}@${host}:${port}?${params.toString()}#${encodeURIComponent(tag)}`;
    }

    if (out.protocol === 'trojan' && server) {
      const password = server.password || '';
      const host = server.address;
      const port = server.port;
      const sni = out.streamSettings?.tlsSettings?.serverName || host;
      const params = new URLSearchParams();
      params.set('sni', sni);
      params.set('security', 'tls');
      return `trojan://${password}@${host}:${port}?${params.toString()}#${encodeURIComponent(tag)}`;
    }

    if ((out.protocol === 'shadowsocks' || out.protocol === 'ss') && server) {
      const method = server.method || 'aes-256-gcm';
      const password = server.password || '';
      const host = server.address;
      const port = server.port;
      const userinfo = Buffer.from(`${method}:${password}`).toString('base64');
      return `ss://${userinfo}@${host}:${port}#${encodeURIComponent(tag)}`;
    }

    return null;
  }

  async isAvailable(): Promise<boolean> {
    try {
      return await fs.pathExists(this.configPath);
    } catch {
      return false;
    }
  }
}
```

- [ ] **Step 1: 创建 adapter 文件并验证类型**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | grep -i "adapter" | head -10
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/server/services/adapters/
git commit -m "feat(adapters): add KernelAdapter interface and SingBox/Xray/V2ray adapters"
```

---

### Task 3: NodeManager 服务

**Files:**
- Create: `frontend/src/server/services/nodeManager.ts`

**Interfaces:**
- Produces: `NodeManager` 单例类
  - `loadNodes(): Promise<NodeConfig[]>` — 读取 nodes.yaml
  - `getClusterStatus(): Promise<ClusterStatus>` — 聚合所有节点状态（缓存 <30s）
  - `triggerUpdate(nodeId?: string): Promise<{results: Record<string,{success:boolean,message:string}>}>` — 触发更新
  - `healthCheck(nodeId?: string): Promise<Record<string,{online:boolean,latency:number}>>` — 健康检查
  - `signRequest(node, method, path, body?): { headers: Record<string,string> }` — HMAC 签名
  - `pollNode(node): Promise<NodeStatus>` — 轮询单节点
- Consumes: `NodeConfig`, `NodeStatus`, `ClusterStatus` from types (Task 1)

**Create:** `frontend/src/server/services/nodeManager.ts`

```typescript
import * as fs from 'fs-extra';
import * as path from 'path';
import * as os from 'os';
import * as crypto from 'crypto';
import ky from 'ky';
import { logger } from '../utils/logger';
import { MioBridgeService } from './mioBridgeService';
import type { NodeConfig, NodeStatus, ClusterStatus, NodesYaml } from '../types';

const NODES_YAML_PATH = path.join(os.homedir(), '.config', 'miobridge', 'nodes.yaml');
const POLL_TIMEOUT = 10000;   // 远程节点请求超时 10s
const CACHE_TTL = 30_000;     // 缓存 30s

interface CacheEntry {
  status: NodeStatus;
  fetchedAt: number;
}

export class NodeManager {
  private static instance: NodeManager;
  private nodes: NodeConfig[] = [];
  private cache: Map<string, CacheEntry> = new Map();
  private watcher: fs.FSWatcher | null = null;
  private localService: MioBridgeService;

  private constructor() {
    this.localService = MioBridgeService.getInstance();
  }

  public static getInstance(): NodeManager {
    if (!NodeManager.instance) {
      NodeManager.instance = new NodeManager();
    }
    return NodeManager.instance;
  }

  /** 初始化：加载节点并启动文件监听 */
  async init(): Promise<void> {
    await this.loadNodes();
    this.startWatcher();
  }

  /** 读取 nodes.yaml */
  async loadNodes(): Promise<NodeConfig[]> {
    try {
      if (!(await fs.pathExists(NODES_YAML_PATH))) {
        this.nodes = [];
        logger.info('NodeManager: nodes.yaml 不存在，运行在单机模式');
        return [];
      }
      const raw = await fs.readFile(NODES_YAML_PATH, 'utf8');
      // 简单 YAML 解析（nodes.yaml 结构简单，避免引入 js-yaml 依赖）
      const parsed = this.parseNodesYaml(raw);
      this.nodes = parsed.nodes.filter(n => n.enabled);
      logger.info(`NodeManager: 加载了 ${this.nodes.length} 个节点`);
      return this.nodes;
    } catch (error: any) {
      logger.error(`NodeManager: 加载 nodes.yaml 失败: ${error.message}`);
      this.nodes = [];
      return [];
    }
  }

  /** 简易 YAML 解析（只解析 nodes 数组） */
  private parseNodesYaml(raw: string): NodesYaml {
    const nodes: NodeConfig[] = [];
    let current: Partial<NodeConfig> = {};
    const lines = raw.split('\n');

    for (const line of lines) {
      const trimmed = line.trim();
      if (trimmed.startsWith('#') || trimmed === '') continue;

      if (trimmed.startsWith('- id:')) {
        if (current.id) { nodes.push(current as NodeConfig); }
        current = { id: this.extractYamlValue(trimmed, 'id') };
      } else if (trimmed.startsWith('name:')) {
        current.name = this.extractYamlValue(trimmed, 'name');
      } else if (trimmed.startsWith('host:')) {
        current.host = this.extractYamlValue(trimmed, 'host');
      } else if (trimmed.startsWith('port:')) {
        current.port = parseInt(this.extractYamlValue(trimmed, 'port')) || 443;
      } else if (trimmed.startsWith('secret:')) {
        current.secret = this.extractYamlValue(trimmed, 'secret');
      } else if (trimmed.startsWith('kernel:')) {
        current.kernel = this.extractYamlValue(trimmed, 'kernel') as NodeConfig['kernel'];
      } else if (trimmed.startsWith('location:')) {
        current.location = this.extractYamlValue(trimmed, 'location');
      } else if (trimmed.startsWith('enabled:')) {
        current.enabled = this.extractYamlValue(trimmed, 'enabled') !== 'false';
      }
    }
    if (current.id) { nodes.push(current as NodeConfig); }
    return { nodes };
  }

  private extractYamlValue(line: string, _key: string): string {
    const colonIdx = line.indexOf(':');
    if (colonIdx === -1) return '';
    let val = line.substring(colonIdx + 1).trim();
    // 移除引号
    if ((val.startsWith('"') && val.endsWith('"')) ||
        (val.startsWith("'") && val.endsWith("'"))) {
      val = val.slice(1, -1);
    }
    return val;
  }

  /** 文件监听：nodes.yaml 变更时自动重载 */
  private startWatcher(): void {
    try {
      const dir = path.dirname(NODES_YAML_PATH);
      const basename = path.basename(NODES_YAML_PATH);
      this.watcher = fs.watch(dir, async (_event, filename) => {
        if (filename === basename) {
          logger.info('NodeManager: 检测到 nodes.yaml 变更，重新加载...');
          this.cache.clear();
          await this.loadNodes();
        }
      });
      this.watcher.on('error', (err) => {
        logger.warn(`NodeManager: 文件监听错误: ${err.message}`);
      });
    } catch (error: any) {
      logger.warn(`NodeManager: 无法启动文件监听: ${error.message}`);
    }
  }

  /** 检查是否有远程节点 */
  hasRemoteNodes(): boolean {
    return this.nodes.length > 0;
  }

  /** 聚合集群状态 */
  async getClusterStatus(): Promise<ClusterStatus> {
    // 检查缓存
    const now = Date.now();
    const allCached = this.nodes.every(n => {
      const entry = this.cache.get(n.id);
      return entry && (now - entry.fetchedAt) < CACHE_TTL;
    });

    if (allCached && this.nodes.length > 0) {
      const cachedNodes = this.nodes.map(n => this.cache.get(n.id)!.status);
      return this.buildClusterStatus(cachedNodes);
    }

    // 并发轮询所有远程节点
    const remoteStatuses = await Promise.all(
      this.nodes.map(n => this.pollNode(n))
    );

    // 获取本地节点状态
    const localStatus = await this.getLocalNodeStatus();

    const allStatuses = [localStatus, ...remoteStatuses];
    return this.buildClusterStatus(allStatuses);
  }

  /** 构建本地节点状态 */
  private async getLocalNodeStatus(): Promise<NodeStatus> {
    try {
      const status = await this.localService.getStatus();
      return {
        nodeId: 'local',
        name: '本地',
        kernel: 'sing-box',
        location: '本地',
        online: true,
        latency: 0,
        nodesCount: status.nodesCount || 0,
        subscriptionExists: status.subscriptionExists,
        clashExists: status.clashExists,
        mihomoAvailable: status.mihomoAvailable,
        kernelAccessible: status.singBoxAccessible,
        version: status.version,
        uptime: status.uptime,
      };
    } catch (error: any) {
      return {
        nodeId: 'local',
        name: '本地',
        kernel: 'sing-box',
        location: '本地',
        online: false,
        error: error.message,
      };
    }
  }

  /** 轮询单个远程节点 */
  async pollNode(node: NodeConfig): Promise<NodeStatus> {
    const start = Date.now();
    try {
      const headers = this.signRequest(node, 'GET', '/api/status');
      const response = await ky.get(`https://${node.host}:${node.port}/api/status`, {
        headers,
        timeout: POLL_TIMEOUT,
        retry: 0,
      }).json<{ success: boolean; data: any }>();

      const latency = Date.now() - start;
      const data = response.data || {};

      const status: NodeStatus = {
        nodeId: node.id,
        name: node.name,
        kernel: node.kernel,
        location: node.location,
        online: true,
        latency,
        nodesCount: data.nodesCount,
        subscriptionExists: data.subscriptionExists,
        clashExists: data.clashExists,
        mihomoAvailable: data.mihomoAvailable,
        kernelAccessible: data.singBoxAccessible,
        version: data.version,
        uptime: data.uptime,
      };

      this.cache.set(node.id, { status, fetchedAt: Date.now() });
      return status;
    } catch (error: any) {
      const latency = Date.now() - start;
      const status: NodeStatus = {
        nodeId: node.id,
        name: node.name,
        kernel: node.kernel,
        location: node.location,
        online: false,
        latency,
        error: error.message?.includes('timeout') ? '超时' :
               error.message?.includes('401') ? '认证失败' : error.message,
      };
      this.cache.set(node.id, { status, fetchedAt: Date.now() });
      return status;
    }
  }

  /** 构建 ClusterStatus */
  private buildClusterStatus(allStatuses: NodeStatus[]): ClusterStatus {
    const onlineNodes = allStatuses.filter(n => n.online);
    const totalProxies = allStatuses.reduce((sum, n) => sum + (n.nodesCount || 0), 0);
    return {
      totalNodes: allStatuses.length,
      onlineNodes: onlineNodes.length,
      totalProxies,
      nodes: allStatuses,
      lastUpdated: new Date().toISOString(),
    };
  }

  /** 触发更新（不传 nodeId 则全部） */
  async triggerUpdate(nodeId?: string): Promise<{
    results: Record<string, { success: boolean; message: string }>;
  }> {
    const results: Record<string, { success: boolean; message: string }> = {};

    if (!nodeId || nodeId === 'local') {
      // 先更新本地
      try {
        const result = await this.localService.updateSubscription();
        results['local'] = { success: true, message: result.message };
      } catch (error: any) {
        results['local'] = { success: false, message: error.message };
      }
    }

    const targets = nodeId
      ? this.nodes.filter(n => n.id === nodeId)
      : this.nodes;

    const remoteResults = await Promise.all(
      targets.map(async (n) => {
        try {
          const headers = this.signRequest(n, 'POST', '/api/update');
          const response = await ky.post(`https://${n.host}:${n.port}/api/update`, {
            headers,
            timeout: 120_000,  // 更新可能耗时较长
            retry: 0,
          }).json<{ success: boolean; data: any }>();
          return { id: n.id, success: true, message: response.data?.message || '更新成功' };
        } catch (error: any) {
          return { id: n.id, success: false, message: error.message };
        }
      })
    );

    for (const r of remoteResults) {
      results[r.id] = { success: r.success, message: r.message };
    }

    return { results };
  }

  /** 健康检查 */
  async healthCheck(nodeId?: string): Promise<
    Record<string, { online: boolean; latency: number }>
  > {
    const results: Record<string, { online: boolean; latency: number }> = {};

    // 本地
    try {
      const start = Date.now();
      await this.localService.getStatus();
      results['local'] = { online: true, latency: Date.now() - start };
    } catch {
      results['local'] = { online: false, latency: 0 };
    }

    const targets = nodeId
      ? this.nodes.filter(n => n.id === nodeId)
      : this.nodes;

    const remoteResults = await Promise.all(
      targets.map(async (n) => {
        const start = Date.now();
        try {
          const headers = this.signRequest(n, 'GET', '/api/health');
          await ky.get(`https://${n.host}:${n.port}/api/health`, {
            headers,
            timeout: POLL_TIMEOUT,
            retry: 0,
          });
          return { id: n.id, online: true, latency: Date.now() - start };
        } catch {
          return { id: n.id, online: false, latency: Date.now() - start };
        }
      })
    );

    for (const r of remoteResults) {
      results[r.id] = r;
    }

    return results;
  }

  /** HMAC-SHA256 签名 */
  signRequest(
    node: NodeConfig,
    method: string,
    reqPath: string,
    body?: string,
  ): Record<string, string> {
    // localhost 节点不签名
    if (node.host === 'localhost' || node.host === '127.0.0.1') {
      return {};
    }
    const timestamp = Date.now().toString();
    const payload = `${timestamp}\n${method}\n${reqPath}\n${body ?? ''}`;
    const signature = crypto
      .createHmac('sha256', node.secret || '')
      .update(payload)
      .digest('hex');
    return {
      'X-Node-Id': node.id,
      'X-Timestamp': timestamp,
      'X-Signature': signature,
    };
  }

  /** 销毁文件监听 */
  destroy(): void {
    if (this.watcher) {
      this.watcher.close();
      this.watcher = null;
    }
  }
}
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | grep -i "nodeManager" | head -10
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/server/services/nodeManager.ts
git commit -m "feat: add NodeManager service with HMAC signing, polling, and cluster status"
```

---

### Task 4: HMAC 验证中间件

**Files:**
- Create: `frontend/src/server/middleware/hmac.ts`

**Interfaces:**
- Produces: `hmacVerify(req: NextApiRequest): { valid: boolean; error?: string }` — 中间件函数
- Consumes: `NodeConfig` 概念（读取 config.yaml 的 node.secret）

**Create:** `frontend/src/server/middleware/hmac.ts`

```typescript
import type { NextApiRequest } from 'next';
import * as crypto from 'crypto';

const TIME_WINDOW_MS = 30_000; // ±30s

/** 已使用的时间戳集合（防重放），每 60s 清理 */
const usedTimestamps = new Set<string>();
let lastCleanup = Date.now();

function cleanupTimestamps(): void {
  const now = Date.now();
  if (now - lastCleanup < 60_000) return;
  usedTimestamps.clear();
  lastCleanup = now;
}

/**
 * 验证 HMAC 签名
 * @param req NextApiRequest
 * @param secret 共享密钥（64 字符 hex）
 * @returns 验证结果
 */
export function hmacVerify(
  req: NextApiRequest,
  secret: string,
): { valid: boolean; error?: string } {
  // 1. localhost 跳过验证
  const remoteAddr = req.socket?.remoteAddress || req.headers['x-forwarded-for'] || '';
  if (remoteAddr === '127.0.0.1' || remoteAddr === '::1' || remoteAddr === 'localhost') {
    return { valid: true };
  }

  // 2. 提取请求头
  const nodeId = req.headers['x-node-id'] as string;
  const timestamp = req.headers['x-timestamp'] as string;
  const signature = req.headers['x-signature'] as string;

  if (!nodeId || !timestamp || !signature) {
    return { valid: false, error: '缺少 HMAC 认证头' };
  }

  // 3. 检查时间窗口
  const reqTime = parseInt(timestamp, 10);
  const now = Date.now();
  if (isNaN(reqTime) || Math.abs(now - reqTime) > TIME_WINDOW_MS) {
    return { valid: false, error: `时间戳超出窗口 (${TIME_WINDOW_MS / 1000}s)` };
  }

  // 4. 防重放
  cleanupTimestamps();
  if (usedTimestamps.has(timestamp)) {
    return { valid: false, error: '重放请求' };
  }
  usedTimestamps.add(timestamp);

  // 5. 重新计算签名并比对
  const method = req.method || 'GET';
  const path = req.url || '/';
  const body = req.body ? JSON.stringify(req.body) : '';
  const payload = `${timestamp}\n${method}\n${path}\n${body}`;

  const expected = crypto
    .createHmac('sha256', secret)
    .update(payload)
    .digest('hex');

  try {
    if (!crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expected))) {
      return { valid: false, error: '签名不匹配' };
    }
  } catch {
    return { valid: false, error: '签名格式错误' };
  }

  return { valid: true };
}

/**
 * 从 config.yaml 获取本节点的 HMAC secret
 * 如果未配置 node.secret，则所有远程请求均失败
 */
export function getNodeSecret(): string | null {
  try {
    // 运行时动态导入，避免循环依赖
    const { yamlService } = require('../services/yamlService');
    const fullConfig = yamlService.getFullConfig() || {};
    return fullConfig.node?.secret || null;
  } catch {
    return null;
  }
}
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | grep -i "hmac" | head -10
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/server/middleware/hmac.ts
git commit -m "feat: add HMAC verification middleware with replay protection"
```

---

### Task 5: 集群 API 路由

**Files:**
- Create: `frontend/src/pages/api/cluster/status.ts`
- Create: `frontend/src/pages/api/cluster/update.ts`
- Create: `frontend/src/pages/api/cluster/health.ts`

**Interfaces:**
- Consumes: `NodeManager` from Task 3, `ApiResponse` from types

#### 5a: GET /api/cluster/status

**Create:** `frontend/src/pages/api/cluster/status.ts`

```typescript
import type { NextApiRequest, NextApiResponse } from 'next';
import { NodeManager } from '@/server/services/nodeManager';
import type { ApiResponse } from '@/server/types';

export default async function handler(
  req: NextApiRequest,
  res: NextApiResponse<ApiResponse>,
) {
  try {
    const clusterStatus = await NodeManager.getInstance().getClusterStatus();
    res.json({
      success: true,
      data: clusterStatus,
      timestamp: new Date().toISOString(),
    });
  } catch (error: any) {
    res.status(500).json({
      success: false,
      error: error.message,
      timestamp: new Date().toISOString(),
    });
  }
}
```

#### 5b: POST /api/cluster/update

**Create:** `frontend/src/pages/api/cluster/update.ts`

```typescript
import type { NextApiRequest, NextApiResponse } from 'next';
import { NodeManager } from '@/server/services/nodeManager';
import { logger } from '@/server/utils/logger';
import type { ApiResponse } from '@/server/types';

export default async function handler(
  req: NextApiRequest,
  res: NextApiResponse<ApiResponse>,
) {
  try {
    const nodeId = (req.query.node as string) || undefined;
    const result = await NodeManager.getInstance().triggerUpdate(nodeId);
    const successCount = Object.values(result.results).filter(r => r.success).length;
    const totalCount = Object.keys(result.results).length;
    res.json({
      success: successCount === totalCount,
      data: result,
      message: `${successCount}/${totalCount} 节点更新成功`,
      timestamp: new Date().toISOString(),
    });
  } catch (error: any) {
    logger.error('集群更新失败:', error);
    res.status(500).json({
      success: false,
      error: error.message,
      timestamp: new Date().toISOString(),
    });
  }
}
```

#### 5c: GET /api/cluster/health

**Create:** `frontend/src/pages/api/cluster/health.ts`

```typescript
import type { NextApiRequest, NextApiResponse } from 'next';
import { NodeManager } from '@/server/services/nodeManager';
import type { ApiResponse } from '@/server/types';

export default async function handler(
  req: NextApiRequest,
  res: NextApiResponse<ApiResponse>,
) {
  try {
    const nodeId = (req.query.node as string) || undefined;
    const result = await NodeManager.getInstance().healthCheck(nodeId);
    res.json({
      success: true,
      data: result,
      timestamp: new Date().toISOString(),
    });
  } catch (error: any) {
    res.status(500).json({
      success: false,
      error: error.message,
      timestamp: new Date().toISOString(),
    });
  }
}
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | grep -i "cluster" | head -10
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/pages/api/cluster/
git commit -m "feat(api): add /api/cluster/status, /api/cluster/update, /api/cluster/health"
```

---

### Task 6: 为现有 API 添加 HMAC 验证

**Files:**
- Modify: `frontend/src/pages/api/status.ts`
- Modify: `frontend/src/pages/api/update.ts`
- Modify: `frontend/src/pages/api/health.ts`

**Interfaces:**
- Consumes: `hmacVerify`, `getNodeSecret` from Task 4

#### 6a: 修改 status.ts

Replace the file content:

```typescript
import type { NextApiRequest, NextApiResponse } from 'next';
import { MioBridgeService } from '@/server/services/mioBridgeService';
import { hmacVerify, getNodeSecret } from '@/server/middleware/hmac';
import type { ApiResponse } from '@/server/types';

export default async function handler(req: NextApiRequest, res: NextApiResponse<ApiResponse>) {
  // HMAC 验证（仅当配置了 node.secret 时生效）
  const secret = getNodeSecret();
  if (secret) {
    const { valid, error } = hmacVerify(req, secret);
    if (!valid) {
      return res.status(401).json({
        success: false,
        error: `认证失败: ${error}`,
        timestamp: new Date().toISOString(),
      });
    }
  }

  try {
    const status = await MioBridgeService.getInstance().getStatus();
    res.json({ success: true, data: status, timestamp: new Date().toISOString() });
  } catch (error: any) {
    res.status(500).json({ success: false, error: error.message, timestamp: new Date().toISOString() });
  }
}
```

#### 6b: 修改 update.ts

Replace the file content:

```typescript
import type { NextApiRequest, NextApiResponse } from 'next';
import { MioBridgeService } from '@/server/services/mioBridgeService';
import { hmacVerify, getNodeSecret } from '@/server/middleware/hmac';
import { logger } from '@/server/utils/logger';
import type { ApiResponse } from '@/server/types';

export const config = {
  maxDuration: 120,
};

export default async function handler(req: NextApiRequest, res: NextApiResponse<ApiResponse>) {
  // HMAC 验证
  const secret = getNodeSecret();
  if (secret) {
    const { valid, error } = hmacVerify(req, secret);
    if (!valid) {
      return res.status(401).json({
        success: false,
        error: `认证失败: ${error}`,
        timestamp: new Date().toISOString(),
      });
    }
  }

  try {
    const result = await MioBridgeService.getInstance().updateSubscription();
    res.json({ success: true, data: result, message: '订阅更新成功', timestamp: new Date().toISOString() });
  } catch (error: any) {
    logger.error('更新订阅API错误:', error);
    res.status(500).json({ success: false, error: error.message, timestamp: new Date().toISOString() });
  }
}
```

#### 6c: 修改 health.ts

Replace the file content:

```typescript
import type { NextApiRequest, NextApiResponse } from 'next';
import { VERSION } from '@/server/version';
import { hmacVerify, getNodeSecret } from '@/server/middleware/hmac';

export default function handler(req: NextApiRequest, res: NextApiResponse) {
  // HMAC 验证
  const secret = getNodeSecret();
  if (secret) {
    const { valid, error } = hmacVerify(req, secret);
    if (!valid) {
      return res.status(401).json({
        status: 'unhealthy',
        error: `认证失败: ${error}`,
        timestamp: new Date().toISOString(),
      });
    }
  }

  try {
    res.json({
      status: 'healthy',
      timestamp: new Date().toISOString(),
      uptime: process.uptime(),
      memory: process.memoryUsage(),
      version: VERSION,
    });
  } catch (error: any) {
    res.status(503).json({ status: 'unhealthy', error: error.message, timestamp: new Date().toISOString() });
  }
}
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | head -20
```
Expected: No errors (all modified files compile cleanly).

- [ ] **Step 2: Commit**

```bash
git add frontend/src/pages/api/status.ts frontend/src/pages/api/update.ts frontend/src/pages/api/health.ts
git commit -m "feat(api): add HMAC verification to existing /api/status, /api/update, /api/health"
```

---

### Task 7: 启动时初始化 NodeManager

**Files:**
- Modify: `frontend/src/instrumentation-node.ts`

**Interfaces:**
- Consumes: `NodeManager` from Task 3

在现有的 `instrumentation-node.ts` 中，在 `ensureDirectories()` 之后添加 NodeManager 初始化。

找到以下位置（大约在 `ensureDirectories` 调用后）：

```typescript
// 在 ensureDirectories() 之后添加：
// 初始化 NodeManager（非阻塞，nodes.yaml 缺失时自动跳过）
import { NodeManager } from './server/services/nodeManager';
NodeManager.getInstance().init().catch((err: Error) => {
  logger.warn(`NodeManager 初始化失败（可能无 nodes.yaml）: ${err.message}`);
});
```

**具体修改：** 打开 `frontend/src/instrumentation-node.ts`，在文件顶部添加 import：

```typescript
import { NodeManager } from './server/services/nodeManager';
```

然后在 `ensureDirectories()` 的 `await` 调用之后（约在第 15-20 行区域），添加：

```typescript
// 初始化 NodeManager（用于多节点管理，单机部署可跳过）
NodeManager.getInstance().init().catch((err: Error) => {
  logger.warn(`NodeManager 初始化失败（正常，如果无 nodes.yaml）: ${err.message}`);
});
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | head -10
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/instrumentation-node.ts
git commit -m "feat: initialize NodeManager on startup"
```

---

### Task 8: Dashboard 集群概览组件 (ClusterOverview)

**Files:**
- Create: `frontend/src/components/cluster/ClusterOverview.tsx`

**Interfaces:**
- Consumes: `ClusterStatus` type (via props)
- Produces: React component — 全局统计条

**Create:** `frontend/src/components/cluster/ClusterOverview.tsx`

```typescript
"use client";

import { Icon } from '@iconify/react';
import type { ClusterStatus } from '@/server/types';

interface ClusterOverviewProps {
  cluster: ClusterStatus;
}

export default function ClusterOverview({ cluster }: ClusterOverviewProps) {
  return (
    <section>
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 stagger-slide-up">
        <StatCard
          label="总节点数"
          value={cluster.totalNodes}
          sub="已注册节点"
          icon="ph:network-bold"
          status="info"
        />
        <StatCard
          label="在线节点"
          value={cluster.onlineNodes}
          sub={`${cluster.totalNodes > 0 ? Math.round(cluster.onlineNodes / cluster.totalNodes * 100) : 0}% 在线率`}
          icon="ph:wifi-high-bold"
          status={cluster.onlineNodes === cluster.totalNodes ? 'success' : cluster.onlineNodes > 0 ? 'warning' : 'danger'}
        />
        <StatCard
          label="总代理数"
          value={cluster.totalProxies}
          sub="全部节点代理合计"
          icon="ph:tree-structure-bold"
          status={cluster.totalProxies > 0 ? 'success' : 'warning'}
        />
        <StatCard
          label="最后更新"
          value={formatTimeAgo(cluster.lastUpdated)}
          sub="状态聚合时间"
          icon="ph:clock-bold"
          status="info"
        />
      </div>
    </section>
  );
}

function StatCard({ label, value, sub, icon, status }: {
  label: string;
  value: string | number;
  sub: string;
  icon: string;
  status: 'success' | 'warning' | 'danger' | 'info';
}) {
  const borderColors: Record<string, string> = {
    success: 'var(--fern)',
    warning: 'var(--marigold)',
    danger: 'var(--terracotta)',
    info: 'var(--primary)',
  };

  return (
    <div className="garden-card p-5" style={{ borderLeft: `3px solid ${borderColors[status]}` }}>
      <div className="flex items-center justify-between mb-2">
        <span className="text-xs uppercase tracking-wider" style={{ color: 'var(--muted-foreground)' }}>
          {label}
        </span>
        <Icon icon={icon} className="w-5 h-5" style={{ color: 'var(--muted-foreground)' }} />
      </div>
      <div className="text-2xl font-semibold" style={{ fontFamily: 'var(--font-display)' }}>
        {value}
      </div>
      <div className="text-xs mt-1" style={{ color: 'var(--muted-foreground)' }}>
        {sub}
      </div>
    </div>
  );
}

function formatTimeAgo(iso: string): string {
  const diff = Date.now() - new Date(iso).getTime();
  const seconds = Math.floor(diff / 1000);
  if (seconds < 5) return '刚刚';
  if (seconds < 60) return `${seconds}s 前`;
  const minutes = Math.floor(seconds / 60);
  return `${minutes}m 前`;
}
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | grep -i "ClusterOverview" | head -10
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/cluster/ClusterOverview.tsx
git commit -m "feat(ui): add ClusterOverview component for multi-node stats"
```

---

### Task 9: Dashboard 节点卡片组件 (NodeCard)

**Files:**
- Create: `frontend/src/components/cluster/NodeCard.tsx`

**Interfaces:**
- Consumes: `NodeStatus` type
- Produces: React component — 单节点卡片

**Create:** `frontend/src/components/cluster/NodeCard.tsx`

```typescript
"use client";

import { Icon } from '@iconify/react';
import type { NodeStatus } from '@/server/types';
import StatusBadge from '@/components/shared/StatusBadge';

interface NodeCardProps {
  node: NodeStatus;
  onClick?: (nodeId: string) => void;
  isActive?: boolean;
}

const kernelLabels: Record<string, string> = {
  'sing-box': 'Sing-Box',
  'xray': 'Xray',
  'v2ray': 'V2Ray',
};

const kernelIcons: Record<string, string> = {
  'sing-box': 'ph:cube-bold',
  'xray': 'ph:lightning-bold',
  'v2ray': 'ph:rocket-launch-bold',
};

export default function NodeCard({ node, onClick, isActive }: NodeCardProps) {
  return (
    <div
      className={`garden-card p-5 cursor-pointer transition-all duration-200 ${
        isActive ? 'ring-2' : ''
      }`}
      style={{
        borderColor: isActive ? 'var(--fern)' : 'var(--border)',
      }}
      onClick={() => onClick?.(node.nodeId)}
    >
      {/* 头部：名称 + 状态 */}
      <div className="flex items-center justify-between mb-3">
        <div className="flex items-center gap-2">
          <div
            className={`w-2.5 h-2.5 rounded-full ${
              node.online ? 'live-dot-active' : ''
            }`}
            style={{
              backgroundColor: node.online ? 'var(--fern)' : 'var(--terracotta)',
            }}
          />
          <h3 className="font-semibold text-base" style={{ fontFamily: 'var(--font-display)' }}>
            {node.name}
          </h3>
        </div>
        <StatusBadge
          label={node.online ? '在线' : (node.error || '离线')}
          status={node.online ? 'success' : 'danger'}
        />
      </div>

      {/* 内核 + 位置 */}
      <div className="flex items-center gap-4 mb-3">
        <div className="flex items-center gap-1.5">
          <Icon
            icon={kernelIcons[node.kernel] || 'ph:question-bold'}
            className="w-4 h-4"
            style={{ color: 'var(--muted-foreground)' }}
          />
          <span className="text-xs" style={{ color: 'var(--muted-foreground)' }}>
            {kernelLabels[node.kernel] || node.kernel}
          </span>
        </div>
        <div className="flex items-center gap-1.5">
          <Icon icon="ph:map-pin-bold" className="w-4 h-4" style={{ color: 'var(--muted-foreground)' }} />
          <span className="text-xs" style={{ color: 'var(--muted-foreground)' }}>
            {node.location}
          </span>
        </div>
      </div>

      {/* 统计行 */}
      <div className="grid grid-cols-3 gap-2 text-center">
        <div>
          <div className="text-lg font-semibold" style={{ fontFamily: 'var(--font-display)' }}>
            {node.nodesCount ?? '-'}
          </div>
          <div className="text-[10px] uppercase tracking-wider" style={{ color: 'var(--muted-foreground)' }}>
            代理
          </div>
        </div>
        <div>
          <div className="text-lg font-semibold" style={{ fontFamily: 'var(--font-display)' }}>
            {node.latency != null ? `${node.latency}ms` : '-'}
          </div>
          <div className="text-[10px] uppercase tracking-wider" style={{ color: 'var(--muted-foreground)' }}>
            延迟
          </div>
        </div>
        <div>
          <div className="text-lg font-semibold" style={{ fontFamily: 'var(--font-display)' }}>
            {node.version ?? '-'}
          </div>
          <div className="text-[10px] uppercase tracking-wider" style={{ color: 'var(--muted-foreground)' }}>
            版本
          </div>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | grep -i "NodeCard" | head -10
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/cluster/NodeCard.tsx
git commit -m "feat(ui): add NodeCard component for individual node display"
```

---

### Task 10: Dashboard 节点详情面板 (NodeDetail)

**Files:**
- Create: `frontend/src/components/cluster/NodeDetail.tsx`

**Interfaces:**
- Consumes: `NodeStatus` type
- Produces: React component — 展开的节点详情面板

**Create:** `frontend/src/components/cluster/NodeDetail.tsx`

```typescript
"use client";

import { Icon } from '@iconify/react';
import type { NodeStatus } from '@/server/types';
import StatusBadge from '@/components/shared/StatusBadge';
import SectionHeading from '@/components/shared/SectionHeading';
import InfoRow from '@/components/shared/InfoRow';

interface NodeDetailProps {
  node: NodeStatus;
  onClose: () => void;
  onUpdate: (nodeId: string) => void;
}

export default function NodeDetail({ node, onClose, onUpdate }: NodeDetailProps) {
  return (
    <div className="garden-card p-6 animate-slideUp">
      {/* 头部 */}
      <div className="flex items-center justify-between mb-6">
        <div className="flex items-center gap-3">
          <div
            className={`w-3 h-3 rounded-full ${node.online ? 'live-dot-active' : ''}`}
            style={{ backgroundColor: node.online ? 'var(--fern)' : 'var(--terracotta)' }}
          />
          <h2 className="text-xl font-semibold" style={{ fontFamily: 'var(--font-display)' }}>
            {node.name}
          </h2>
          <StatusBadge
            label={node.online ? '在线' : (node.error || '离线')}
            status={node.online ? 'success' : 'danger'}
          />
        </div>
        <button
          onClick={onClose}
          className="w-8 h-8 flex items-center justify-center rounded-lg hover:bg-opacity-10"
          style={{ color: 'var(--muted-foreground)' }}
          aria-label="关闭详情"
        >
          <Icon icon="ph:x-bold" className="w-5 h-5" />
        </button>
      </div>

      {/* 错误信息 */}
      {node.error && (
        <div className="garden-alert garden-alert-danger mb-4">
          <Icon icon="ph:warning-circle-bold" className="w-5 h-5 flex-shrink-0 mt-0.5" />
          <div>
            <p className="text-sm font-semibold">节点异常</p>
            <p className="text-sm mt-0.5 opacity-90">{node.error}</p>
          </div>
        </div>
      )}

      {/* 基本信息 */}
      <SectionHeading icon="ph:info-bold" title="基本信息" />
      <div className="garden-card p-4 mb-4">
        <div className="space-y-1 divide-y" style={{ borderColor: 'var(--border)' }}>
          <InfoRow label="节点 ID">
            <span className="font-mono text-xs">{node.nodeId}</span>
          </InfoRow>
          <InfoRow label="内核类型">
            <span>{node.kernel}</span>
          </InfoRow>
          <InfoRow label="位置">
            <span>{node.location}</span>
          </InfoRow>
          <InfoRow label="延迟">
            <span>{node.latency != null ? `${node.latency}ms` : '-'}</span>
          </InfoRow>
          <InfoRow label="版本">
            <span className="font-mono text-xs">{node.version ?? '-'}</span>
          </InfoRow>
          <InfoRow label="运行时间">
            <span>{node.uptime != null ? formatUptime(node.uptime) : '-'}</span>
          </InfoRow>
        </div>
      </div>

      {/* 服务状态 */}
      <SectionHeading icon="ph:hard-drives-bold" title="服务状态" />
      <div className="garden-card p-4 mb-4">
        <div className="space-y-1 divide-y" style={{ borderColor: 'var(--border)' }}>
          <InfoRow label="Mihomo">
            <StatusBadge
              label={node.mihomoAvailable ? '可用' : '不可用'}
              status={node.mihomoAvailable ? 'success' : 'danger'}
            />
          </InfoRow>
          <InfoRow label="内核">
            <StatusBadge
              label={node.kernelAccessible ? '可访问' : '不可访问'}
              status={node.kernelAccessible ? 'success' : 'danger'}
            />
          </InfoRow>
          <InfoRow label="订阅文件">
            <StatusBadge
              label={node.subscriptionExists ? '已生成' : '未生成'}
              status={node.subscriptionExists ? 'success' : 'danger'}
            />
          </InfoRow>
          <InfoRow label="Clash 配置">
            <StatusBadge
              label={node.clashExists ? '已生成' : '未生成'}
              status={node.clashExists ? 'success' : 'danger'}
            />
          </InfoRow>
        </div>
      </div>

      {/* 操作按钮 */}
      <div className="flex gap-3">
        <button
          onClick={() => onUpdate(node.nodeId)}
          className="garden-btn px-4 py-2 rounded-lg text-sm font-medium flex items-center gap-2"
          style={{
            backgroundColor: 'var(--primary)',
            color: 'var(--primary-foreground)',
          }}
          disabled={!node.online}
        >
          <Icon icon="ph:arrows-clockwise-bold" className="w-4 h-4" />
          更新订阅
        </button>
      </div>
    </div>
  );
}

function formatUptime(s: number): string {
  if (!s && s !== 0) return '-';
  const h = Math.floor(s / 3600);
  const m = Math.floor((s % 3600) / 60);
  return `${h}h ${m}m`;
}
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | grep -i "NodeDetail" | head -10
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/cluster/NodeDetail.tsx
git commit -m "feat(ui): add NodeDetail panel component"
```

---

### Task 11: Dashboard 批量操作组件 (BatchActions)

**Files:**
- Create: `frontend/src/components/cluster/BatchActions.tsx`

**Interfaces:**
- Consumes: None (callback props)
- Produces: React component — 批量操作栏

**Create:** `frontend/src/components/cluster/BatchActions.tsx`

```typescript
"use client";

import { useState } from 'react';
import { Icon } from '@iconify/react';

interface BatchActionsProps {
  onUpdateAll: () => Promise<void>;
  onHealthCheck: () => Promise<void>;
  isLoading?: boolean;
}

export default function BatchActions({ onUpdateAll, onHealthCheck, isLoading }: BatchActionsProps) {
  const [actionLoading, setActionLoading] = useState<'update' | 'health' | null>(null);
  const [toast, setToast] = useState<{ type: 'success' | 'error'; message: string } | null>(null);

  const handleUpdateAll = async () => {
    setActionLoading('update');
    try {
      await onUpdateAll();
      setToast({ type: 'success', message: '全部节点更新完成' });
    } catch (error: any) {
      setToast({ type: 'error', message: `批量更新失败: ${error.message}` });
    } finally {
      setActionLoading(null);
      setTimeout(() => setToast(null), 4000);
    }
  };

  const handleHealthCheck = async () => {
    setActionLoading('health');
    try {
      await onHealthCheck();
      setToast({ type: 'success', message: '健康检查完成' });
    } catch (error: any) {
      setToast({ type: 'error', message: `健康检查失败: ${error.message}` });
    } finally {
      setActionLoading(null);
      setTimeout(() => setToast(null), 4000);
    }
  };

  return (
    <div className="flex items-center gap-3">
      <button
        onClick={handleUpdateAll}
        disabled={isLoading || actionLoading !== null}
        className="garden-btn px-4 py-2 rounded-lg text-sm font-medium flex items-center gap-2"
        style={{
          backgroundColor: 'var(--primary)',
          color: 'var(--primary-foreground)',
          opacity: isLoading ? 0.6 : 1,
        }}
      >
        <Icon
          icon={actionLoading === 'update' ? 'ph:spinner-bold' : 'ph:arrows-clockwise-bold'}
          className={`w-4 h-4 ${actionLoading === 'update' ? 'animate-spin' : ''}`}
        />
        全部更新
      </button>

      <button
        onClick={handleHealthCheck}
        disabled={isLoading || actionLoading !== null}
        className="garden-btn px-4 py-2 rounded-lg text-sm font-medium flex items-center gap-2"
        style={{
          backgroundColor: 'var(--secondary)',
          color: 'var(--secondary-foreground)',
          border: '1px solid var(--border)',
          opacity: isLoading ? 0.6 : 1,
        }}
      >
        <Icon
          icon={actionLoading === 'health' ? 'ph:spinner-bold' : 'ph:heartbeat-bold'}
          className={`w-4 h-4 ${actionLoading === 'health' ? 'animate-spin' : ''}`}
        />
        健康检查
      </button>

      {/* Toast */}
      {toast && (
        <div
          className={`garden-alert ${
            toast.type === 'success' ? 'garden-alert-success' : 'garden-alert-danger'
          } animate-slideUp`}
          style={{ position: 'fixed', bottom: 24, right: 24, maxWidth: 400, zIndex: 50 }}
        >
          <Icon
            icon={toast.type === 'success' ? 'ph:check-circle-bold' : 'ph:warning-circle-bold'}
            className="w-5 h-5 flex-shrink-0 mt-0.5"
          />
          <p className="text-sm">{toast.message}</p>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | grep -i "BatchActions" | head -10
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/cluster/BatchActions.tsx
git commit -m "feat(ui): add BatchActions component for cluster-wide operations"
```

---

### Task 12: 更新首页支持集群模式

**Files:**
- Modify: `frontend/src/pages/index.tsx`
- Modify: `frontend/src/components/Dashboard.tsx`

**Interfaces:**
- Consumes: `NodeManager` (server-side), `ClusterOverview`, `NodeCard`, `NodeDetail`, `BatchActions`

#### 12a: 修改 index.tsx 的 getServerSideProps

替换 `frontend/src/pages/index.tsx` 为：

```typescript
import type { GetServerSideProps } from 'next';
import Dashboard from '@/components/Dashboard';
import type { ApiStatus, ClusterStatus } from '@/lib/api';

interface HomeProps {
  initialStatus: ApiStatus | null;
  initialCluster: ClusterStatus | null;
  initialError: string | null;
  hasCluster: boolean;
}

export default function Home({ initialStatus, initialCluster, initialError, hasCluster }: HomeProps) {
  return (
    <Dashboard
      initialStatus={initialStatus}
      initialCluster={initialCluster}
      initialError={initialError}
      hasCluster={hasCluster}
    />
  );
}

export const getServerSideProps: GetServerSideProps<HomeProps> = async () => {
  const { MioBridgeService } = await import('@/server/services/mioBridgeService');
  const { NodeManager } = await import('@/server/services/nodeManager');

  try {
    // 检查是否有集群配置
    const nodeManager = NodeManager.getInstance();
    await nodeManager.init(); // 确保 nodes.yaml 已加载

    if (nodeManager.hasRemoteNodes()) {
      const cluster = await nodeManager.getClusterStatus();
      return {
        props: {
          initialStatus: null,
          initialCluster: JSON.parse(JSON.stringify(cluster)) as ClusterStatus,
          initialError: null,
          hasCluster: true,
        },
      };
    }

    // 单机模式
    const status = await MioBridgeService.getInstance().getStatus();
    return {
      props: {
        initialStatus: JSON.parse(JSON.stringify(status)) as ApiStatus,
        initialCluster: null,
        initialError: null,
        hasCluster: false,
      },
    };
  } catch (error) {
    return {
      props: {
        initialStatus: null,
        initialCluster: null,
        initialError: error instanceof Error ? error.message : '获取状态失败',
        hasCluster: false,
      },
    };
  }
};
```

#### 12b: 修改 Dashboard.tsx 支持集群视图

替换 `frontend/src/components/Dashboard.tsx` 为：

```typescript
"use client";

import { Icon } from '@iconify/react';
import { useCallback, useEffect, useState } from 'react';
import { apiService, type ApiStatus, type ClusterStatus } from '@/lib/api';
import StatCard from '@/components/shared/StatCard';
import StatusBadge from '@/components/shared/StatusBadge';
import SectionHeading from '@/components/shared/SectionHeading';
import InfoRow from '@/components/shared/InfoRow';
import ClusterOverview from '@/components/cluster/ClusterOverview';
import NodeCard from '@/components/cluster/NodeCard';
import NodeDetail from '@/components/cluster/NodeDetail';
import BatchActions from '@/components/cluster/BatchActions';

interface DashboardProps {
  initialStatus?: ApiStatus | null;
  initialCluster?: ClusterStatus | null;
  initialError?: string | null;
  hasCluster?: boolean;
}

export default function Dashboard({
  initialStatus = null,
  initialCluster = null,
  initialError = null,
  hasCluster = false,
}: DashboardProps) {
  const [status, setStatus] = useState<ApiStatus | null>(initialStatus);
  const [cluster, setCluster] = useState<ClusterStatus | null>(initialCluster);
  const [error, setError] = useState<string | null>(initialError);
  const [hasAnimated, setHasAnimated] = useState(false);
  const [selectedNodeId, setSelectedNodeId] = useState<string | null>(null);

  const fetchStatus = useCallback(async () => {
    try {
      setError(null);
      if (hasCluster) {
        const c = await apiService.getClusterStatus();
        setCluster(c);
      } else {
        const s = await apiService.getStatus();
        setStatus(s);
      }
    } catch (err) {
      setError(err instanceof Error ? err.message : '获取状态失败');
    }
  }, [hasCluster]);

  useEffect(() => {
    setHasAnimated(true);
    fetchStatus();
    const timer = setInterval(fetchStatus, 30000);
    return () => clearInterval(timer);
  }, [fetchStatus]);

  const selectedNode = cluster?.nodes.find(n => n.nodeId === selectedNodeId) || null;

  const handleUpdateAll = async () => {
    const result = await apiService.triggerClusterUpdate();
    const successCount = Object.values(result.results || {}).filter((r: any) => r.success).length;
    const totalCount = Object.keys(result.results || {}).length;
    if (successCount !== totalCount) {
      throw new Error(`${successCount}/${totalCount} 成功`);
    }
    await fetchStatus();
  };

  const handleHealthCheck = async () => {
    await apiService.clusterHealthCheck();
    await fetchStatus();
  };

  const handleUpdateNode = async (nodeId: string) => {
    await apiService.triggerClusterUpdate(nodeId);
    await fetchStatus();
  };

  const formatUptime = (s: number) => {
    if (!s && s !== 0) return '-';
    const h = Math.floor(s / 3600);
    const m = Math.floor((s % 3600) / 60);
    return `${h}h ${m}m`;
  };

  const formatSize = (b: number) => {
    if (!b) return '-';
    const i = Math.floor(Math.log(b) / Math.log(1024));
    return `${(b / Math.pow(1024, i)).toFixed(1)} ${['B', 'KB', 'MB', 'GB'][i]}`;
  };

  const formatDate = (d: string) => new Date(d).toLocaleString('zh-CN');

  // ==================== 集群视图 ====================
  if (hasCluster && cluster) {
    return (
      <div className="px-6 py-8 max-w-6xl mx-auto space-y-8">
        {/* Error */}
        {error && (
          <div className="garden-alert garden-alert-danger">
            <Icon icon="ph:warning-circle-bold" className="w-5 h-5 flex-shrink-0 mt-0.5" />
            <div>
              <p className="text-sm font-semibold">获取集群状态失败</p>
              <p className="text-sm mt-0.5 opacity-90">{error}</p>
            </div>
          </div>
        )}

        {/* 批量操作 */}
        <section>
          <div className="flex items-center justify-between mb-4">
            <SectionHeading icon="ph:cluster-bold" title="集群总览" desc={`${cluster.nodes.length} 个节点`} />
            <BatchActions
              onUpdateAll={handleUpdateAll}
              onHealthCheck={handleHealthCheck}
            />
          </div>
          <ClusterOverview cluster={cluster} />
        </section>

        {/* 节点卡片 + 详情 */}
        <section>
          <SectionHeading icon="ph:grid-four-bold" title="节点列表" desc="点击卡片查看详情" />
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 stagger-slide-up">
            {cluster.nodes.map(node => (
              <NodeCard
                key={node.nodeId}
                node={node}
                isActive={selectedNodeId === node.nodeId}
                onClick={(id) => setSelectedNodeId(selectedNodeId === id ? null : id)}
              />
            ))}
          </div>
        </section>

        {/* 详情面板 */}
        {selectedNode && (
          <section>
            <NodeDetail
              node={selectedNode}
              onClose={() => setSelectedNodeId(null)}
              onUpdate={handleUpdateNode}
            />
          </section>
        )}

        <footer className="text-center pt-4 pb-8">
          <p className="text-xs" style={{ color: 'var(--muted-foreground)' }}>
            Mio Garden · Multi-Node Cluster · Botanical Garden Theme
          </p>
        </footer>
      </div>
    );
  }

  // ==================== 单机视图（原有逻辑） ====================
  return (
    <div className="px-6 py-8 max-w-5xl mx-auto space-y-8">
      {error && (
        <div className="garden-alert garden-alert-danger">
          <Icon icon="ph:warning-circle-bold" className="w-5 h-5 flex-shrink-0 mt-0.5" />
          <div>
            <p className="text-sm font-semibold">获取状态失败</p>
            <p className="text-sm mt-0.5 opacity-90">{error}</p>
          </div>
        </div>
      )}

      <section>
        <SectionHeading icon="ph:gauge-bold" title="状态概览" desc="服务与订阅文件的实时状态" />
        <div className={`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 ${hasAnimated ? 'stagger-slide-up' : ''}`}>
          <StatCard
            label="订阅文件"
            value={status?.subscriptionExists ? '已生成' : '未生成'}
            sub={status?.subscriptionExists && status?.subscriptionLastUpdated
              ? formatDate(status.subscriptionLastUpdated) : '尚未更新'}
            icon="ph:file-text"
            status={status?.subscriptionExists ? 'success' : 'danger'}
          />
          <StatCard
            label="Clash 配置"
            value={status?.clashExists ? '已生成' : '未生成'}
            sub={status?.clashExists && status?.clashLastUpdated
              ? formatDate(status.clashLastUpdated) : '尚未更新'}
            icon="ph:shield-check"
            status={status?.clashExists ? 'success' : 'danger'}
          />
          <StatCard
            label="节点数量"
            value={status?.nodesCount ?? 0}
            sub="可用节点"
            icon="ph:tree-structure"
            status={status?.nodesCount && status.nodesCount > 0 ? 'success' : 'warning'}
          />
          <StatCard
            label="运行时间"
            value={status ? formatUptime(status.uptime) : '-'}
            sub="服务实例"
            icon="ph:clock"
            status="info"
          />
        </div>
      </section>

      <section>
        <SectionHeading icon="ph:hard-drives-bold" title="服务与文件" desc="依赖服务及生成文件详情" />
        <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div className="garden-card p-5">
            <h3 className="text-xs font-semibold uppercase tracking-widest mb-4" style={{ color: 'var(--muted-foreground)' }}>
              核心服务
            </h3>
            <div className="space-y-1 divide-y" style={{ borderColor: 'var(--border)' }}>
              <InfoRow label="Mihomo">
                <StatusBadge
                  label={status?.mihomoAvailable ? '可用' : '不可用'}
                  status={status?.mihomoAvailable ? 'success' : 'danger'}
                />
                {status?.mihomoVersion && (
                  <span className="text-xs" style={{ color: 'var(--muted-foreground)' }}>
                    v{status.mihomoVersion}
                  </span>
                )}
              </InfoRow>
              <InfoRow label="Sing-box">
                <StatusBadge
                  label={status?.singBoxAccessible ? '可访问' : '不可访问'}
                  status={status?.singBoxAccessible ? 'success' : 'danger'}
                />
              </InfoRow>
              <InfoRow label="API 版本">
                <span className="font-mono text-xs" style={{ color: 'var(--muted-foreground)' }}>
                  {status?.version ?? '-'}
                </span>
              </InfoRow>
            </div>
          </div>

          <div className="garden-card p-5">
            <h3 className="text-xs font-semibold uppercase tracking-widest mb-4" style={{ color: 'var(--muted-foreground)' }}>
              文件信息
            </h3>
            <div className="space-y-1 divide-y" style={{ borderColor: 'var(--border)' }}>
              <InfoRow label="subscription.txt">
                {status?.subscriptionSize ? formatSize(status.subscriptionSize) : '-'}
              </InfoRow>
              <InfoRow label="clash.yaml">
                {status?.clashSize ? formatSize(status.clashSize) : '-'}
              </InfoRow>
              <InfoRow label="raw.txt">
                {status?.rawExists
                  ? <StatusBadge label="已生成" status="success" />
                  : <StatusBadge label="未生成" status="warning" />
                }
              </InfoRow>
            </div>
          </div>
        </div>
      </section>

      <footer className="text-center pt-4 pb-8">
        <p className="text-xs" style={{ color: 'var(--muted-foreground)' }}>
          Mio Garden · Next.js SSR · Botanical Garden Theme
          {status?.version && <> · v{status.version}</>}
        </p>
        {status?.gitCommit && (
          <p className="text-[10px] mt-1 font-mono" style={{ color: 'var(--muted-foreground)' }}>
            {status.gitCommit}
            {status.buildTime && <> · built {new Date(status.buildTime).toLocaleDateString('zh-CN')}</>}
          </p>
        )}
      </footer>
    </div>
  );
}
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | head -20
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/pages/index.tsx frontend/src/components/Dashboard.tsx
git commit -m "feat: add cluster view to Dashboard with automatic single/cluster mode detection"
```

---

### Task 13: API 客户端扩展

**Files:**
- Modify: `frontend/src/lib/api.ts`

**Interfaces:**
- Produces: `ClusterStatus` type (re-export), `getClusterStatus()`, `triggerClusterUpdate(nodeId?)`, `clusterHealthCheck(nodeId?)` methods

在 `frontend/src/lib/api.ts` 文件末尾的 `ApiService` 类中添加以下方法。在 `class ApiService` 的闭合花括号 `}` 之前添加：

```typescript
  // ==================== 集群 API ====================

  /** 获取集群状态 */
  async getClusterStatus(): Promise<ClusterStatus> {
    try {
      const response = await apiClient.get('api/cluster/status').json<ApiResponse<ClusterStatus>>();
      return response.data as ClusterStatus;
    } catch (error) {
      return this.handleError(error);
    }
  }

  /** 触发集群更新（不传 nodeId 则全部） */
  async triggerClusterUpdate(nodeId?: string): Promise<{
    results: Record<string, { success: boolean; message: string }>;
  }> {
    try {
      const query = nodeId ? `?node=${nodeId}` : '';
      const response = await apiClient.post(`api/cluster/update${query}`).json<ApiResponse<any>>();
      return response.data;
    } catch (error) {
      return this.handleError(error);
    }
  }

  /** 集群健康检查 */
  async clusterHealthCheck(nodeId?: string): Promise<
    Record<string, { online: boolean; latency: number }>
  > {
    try {
      const query = nodeId ? `?node=${nodeId}` : '';
      const response = await apiClient.get(`api/cluster/health${query}`).json<ApiResponse<any>>();
      return response.data;
    } catch (error) {
      return this.handleError(error);
    }
  }
```

同时在文件顶部类型定义区域（在 `ApiStatus` 接口之后）添加 ClusterStatus 类型：

```typescript
export interface ClusterStatus {
  totalNodes: number;
  onlineNodes: number;
  totalProxies: number;
  nodes: Array<{
    nodeId: string;
    name: string;
    kernel: string;
    location: string;
    online: boolean;
    error?: string;
    latency?: number;
    nodesCount?: number;
    subscriptionExists?: boolean;
    clashExists?: boolean;
    mihomoAvailable?: boolean;
    kernelAccessible?: boolean;
    version?: string;
    uptime?: number;
  }>;
  lastUpdated: string;
}
```

- [ ] **Step 1: 验证编译**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1 | grep -i "cluster" | head -10
```
Expected: No errors.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/lib/api.ts
git commit -m "feat(api): add cluster API client methods"
```

---

### Task 14: 集成验证

- [ ] **Step 1: 完整类型检查**

```bash
cd /home/imali/subscription-api-ts/frontend && npx tsc --noEmit 2>&1
```
Expected: No errors across the entire project.

- [ ] **Step 2: 构建验证**

```bash
cd /home/imali/subscription-api-ts && bun run build 2>&1 | tail -20
```
Expected: Build succeeds without errors.

- [ ] **Step 3: 创建示例 nodes.yaml 用于测试**

```bash
cat > ~/.config/miobridge/nodes.yaml << 'EOF'
# MioBridge 多节点注册表
# 文件权限应为 600

nodes:
  - id: "node-a"
    name: "本地"
    host: "localhost"
    port: 3001
    secret: ""
    kernel: "sing-box"
    location: "东京"
    enabled: true
EOF
chmod 600 ~/.config/miobridge/nodes.yaml
```

- [ ] **Step 4: 本地运行验证单机模式**

```bash
cd /home/imali/subscription-api-ts && bun run dev &
sleep 5
curl -s http://localhost:3001/api/status | python3 -m json.tool | head -15
```
Expected: 正常返回 status 数据（单机模式，nodes.yaml 只有一个 localhost 节点）。

- [ ] **Step 5: 验证集群 API**

```bash
curl -s http://localhost:3001/api/cluster/status | python3 -m json.tool | head -20
```
Expected: 返回包含本地节点的 cluster status（1 node, local）。

- [ ] **Step 6: 验证无 nodes.yaml 的向后兼容**

```bash
mv ~/.config/miobridge/nodes.yaml ~/.config/miobridge/nodes.yaml.bak
curl -s http://localhost:3001/api/status | python3 -m json.tool | head -10
# 恢复
mv ~/.config/miobridge/nodes.yaml.bak ~/.config/miobridge/nodes.yaml
```
Expected: 无 nodes.yaml 时行为与之前完全一致，不报错。

- [ ] **Step 7: 清理并提交**

```bash
kill %1 2>/dev/null  # 停止 dev server
```

- [ ] **Step 8: 最终提交**

```bash
git add -A
git commit -m "feat: complete v1.0 multi-node distributed management system

- Add NodeManager service with nodes.yaml loading and HMAC signing
- Add KernelAdapter interface and SingBox/Xray/V2ray adapters
- Add HMAC verification middleware with replay protection
- Add /api/cluster/status, /api/cluster/update, /api/cluster/health
- Add HMAC auth to existing /api/status, /api/update, /api/health
- Add ClusterOverview, NodeCard, NodeDetail, BatchActions components
- Update Dashboard with automatic single/cluster mode detection
- Add cluster API client methods
- Backward compatible: no nodes.yaml means single-node mode"
```
```bash
git push origin main
```

---

